// Store/Reducers/AuthReducer.js

const initialState = { isAuth: true };

function Auth(state = initialState, action) {
    let nextState;
    switch (action.type) {
      // case
    default:
      return state;
    }
}

export default Auth;